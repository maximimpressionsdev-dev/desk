DATABASE_URL=postgresql://tickets:tickets@localhost:5432/tickets
AUTH_SECRET=change-me-to-a-long-random-string
AUTH_URL=http://localhost:3000

ADMIN_EMAIL=admin@company.local
ADMIN_PASSWORD=admin12345
ADMIN_NAME=System Admin

# Email (optional in local — logs to console if unset)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=tickets@company.local

# Local file uploads (Docker volume)
UPLOAD_DIR=./uploads
MAX_UPLOAD_BYTES=10485760

NEXT_PUBLIC_APP_NAME=Company Tickets
